object workSheetTest {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(66); 
  println("Welcome to the Scala worksheet");$skip(9); 
	def x=1;System.out.println("""x: => Int""");$skip(5); val res$0 = 
	x+2;System.out.println("""res0: Int = """ + $show(res$0))}
}
